# Copy to .env and fill in real values. Never commit .env.

# Bearer token used as a fallback target auth by rtaas.eval_engine.target.TargetModel
# when no --target-auth is passed on the CLI or to Evaluator(target_auth=...).
OPENAI_API_KEY=
